<!DOCTYPE html>
<html>
  <head>
    <link href="/assets/styles.css" rel = "stylesheet" type="text/css"/>
  </head>
  <body>

    <h1>Contact Me</h1>
    <p>You wont find a website better anywhere else!</p>

    <form id = "contact-form">
      <label for = "Who">Who do you want to contact?</label>
      <input type="text" name = "who" value="<%= data.age %>">
      <label for="department">Which department?</label>
      <input type="text" name="department" value="<%= data.age %>">
      <label for="email">Your email</label>
      <input type="email" name="email"/>
      <input type="submit" value="submit"/>
    </form>

    <ul>
      <% data.docs.forEach(function(item){%>
        <li><%= item %></li>
      <%});%>
    </ul>
  </body>
</html>
